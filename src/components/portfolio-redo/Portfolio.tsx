import PortfolioCards from "./PortfolioCards";

const Portfolio = () => {
  // Cards will be diplayed and linked to slugs in portfolio i.e /portfolio/:slug
  return <PortfolioCards />;
};
export default Portfolio;
